# Minimum-Error-Rate-Classifier(using naive bayes method)
Implementation of Minimum Error Rate Classifier for which .csv file is used as dataset. Lastly a graph is plotted to distinguish between classes.
